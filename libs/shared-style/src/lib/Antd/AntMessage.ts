import { theme } from '../Theme';

import { createGlobalStyle } from 'styled-components';

export const AntMessage = createGlobalStyle`

.ant-message{
font-family: ${theme?.font?.family?.GentiumBasic}
}
`;
