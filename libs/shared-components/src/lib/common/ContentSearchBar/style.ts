import { styled } from 'styled-components';

export const Wrapper = styled.div`
  .searchBarContainer {
    display: inline-flex;
    align-items: center;
  }

  .searchInput {
    width: 395px;
    margin-right: 5px;
  }
`;
